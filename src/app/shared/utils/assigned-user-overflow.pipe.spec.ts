import { AssignedUserOverflowPipe } from './assigned-user-overflow.pipe';

describe('AssignedUserOverflowPipe', () => {
  it('create an instance', () => {
    const pipe = new AssignedUserOverflowPipe();
    expect(pipe).toBeTruthy();
  });
});
