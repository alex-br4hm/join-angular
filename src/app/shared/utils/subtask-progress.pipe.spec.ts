import { SubtaskProgressPipe } from './subtask-progress.pipe';

describe('SubtaskProgressPipe', () => {
  it('create an instance', () => {
    const pipe = new SubtaskProgressPipe();
    expect(pipe).toBeTruthy();
  });
});
