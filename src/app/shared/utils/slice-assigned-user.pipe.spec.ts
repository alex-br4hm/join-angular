import { SliceAssignedUserPipe } from './slice-assigned-user.pipe';

describe('SliceAssignedUserPipe', () => {
  it('create an instance', () => {
    const pipe = new SliceAssignedUserPipe();
    expect(pipe).toBeTruthy();
  });
});
